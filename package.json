{
  "name": "cgae-arc",
  "version": "0.1.0",
  "description": "Comprehension-Gated Agent Economy on Arc — RFB 01 Perps Trading Agent",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network arc"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "5.0.0",
    "@openzeppelin/contracts": "5.3.0",
    "hardhat": "2.28.0"
  }
}
